---
// Deprecated (2026-09-11): legacy portfolio (metaverse projects) retired as part of the
// pivot to live-event gamification experiences. Redirects to home.
import { getLocaleStaticPaths } from "../../../i18n";

export const getStaticPaths = getLocaleStaticPaths;

return Astro.redirect("/", 302);
---

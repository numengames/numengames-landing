---
// Deprecated (2026-09-11): legacy localized home (metaverse/3D-worlds
// messaging) retired as part of the pivot to live-event gamification
// experiences. Redirects to the apex Coming Soon page, which carries the
// current, accurate message.
export async function getStaticPaths() {
	return [{ params: { locale: "en" } }, { params: { locale: "es" } }];
}

return Astro.redirect("/", 302);
---
